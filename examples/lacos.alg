algoritmo "Lacos"
var
  i, soma: inteiro
inicio
  soma <- 0
  para i de 1 ate 5 faca
    soma <- soma + i
  fimpara
  escreval("Soma: ", soma)

  i <- 1
  enquanto i <= 3 faca
    escreval("Contador: ", i)
    i <- i + 1
  fimenquanto
fimalgoritmo
